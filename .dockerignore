**/node_modules
**/dist
.git
.claude
.planning

# Native platform builds not needed for web Docker image
engines/kmp-engine
engines/android-app

# Docs and tests
docs
tests
*.docx
LICENSE
*.md
!engines/web/README.md
!engines/web-ui/README.md

# Keep TypeScript incremental build state out of the image: a stale tsbuildinfo
# plus a stripped dist makes `tsc --build` skip re-emitting .d.ts files.
**/*.tsbuildinfo
