---
phase: 06-home-active-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/screens/HomeScreen.tsx
  - src/components/screens/HomeScreen.module.css
autonomous: true

must_haves:
  truths:
    - "User can load a workflow file via file picker button"
    - "User can load a workflow file via drag-and-drop"
    - "Loaded master workflows appear in a list with workflow name"
    - "Duplicate workflow loading shows an error message"
    - "Empty state shows instructional message with prominent load button"
    - "Loading errors display to the user"
  artifacts:
    - path: "src/components/screens/HomeScreen.tsx"
      provides: "Full Home screen with file loading, workflow list, empty state"
      exports: ["HomeScreen"]
    - path: "src/components/screens/HomeScreen.module.css"
      provides: "Home screen styles with container queries"
  key_links:
    - from: "src/components/screens/HomeScreen.tsx"
      to: "src/manager/fileProcessing.ts"
      via: "processWorkflowFile import"
      pattern: "processWorkflowFile"
    - from: "src/components/screens/HomeScreen.tsx"
      to: "src/manager/useWorkflowManager.ts"
      via: "useWorkflowManager + useManagerSnapshot hooks"
      pattern: "useWorkflowManager|useManagerSnapshot"
    - from: "src/components/screens/HomeScreen.tsx"
      to: "src/manager/WorkflowManager.ts"
      via: "manager.addWorkflow + manager.startWorkflow"
      pattern: "addWorkflow|startWorkflow"
---

<objective>
Build the Home screen where users load master workflow files and see them in a list.

Purpose: This is the entry point for all workflow interaction -- users load .WFmasterX or .json files, see them listed, and can start them.
Output: Fully functional HomeScreen component replacing the current placeholder.
</objective>

<execution_context>
@C:\Users\dnbra\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dnbra\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-home-active-commands/06-CONTEXT.md
@.planning/phases/06-home-active-commands/06-RESEARCH.md
@src/manager/fileProcessing.ts
@src/manager/WorkflowManager.ts
@src/manager/types.ts
@src/manager/useWorkflowManager.ts
@src/components/screens/HomeScreen.tsx
@src/components/screens/PlaceholderScreen.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeScreen component with file loading and workflow list</name>
  <files>
    src/components/screens/HomeScreen.tsx
    src/components/screens/HomeScreen.module.css
  </files>
  <action>
Replace the placeholder HomeScreen with a full implementation:

**File loading:**
- "+ Load Workflow" button that opens a hidden file input (accept=".WFmasterX,.json,.WFmaster")
- Use a ref to the hidden input, click it programmatically on button press
- Reset input value after processing (inputRef.current.value = '') so same file can be loaded twice
- Drag-and-drop zone covering the screen area:
  - preventDefault + stopPropagation on dragenter, dragover, dragleave, drop
  - Visual feedback: dragOver state toggles a CSS class for drop zone highlight
  - Process dropped files via processWorkflowFile from src/manager/fileProcessing.ts
- For each file: call processWorkflowFile, check isProcessingError, then manager.addWorkflow
- Handle addWorkflow result: if 'duplicateId' in result, show "This workflow is already loaded" error
- Error state: useState<string | null> for error messages, cleared on next successful load

**Workflow list:**
- Use useManagerSnapshot() to get the list of workflows
- Each row: workflow name (from managed.name), a "Start" button (calls manager.startWorkflow(id))
- Started workflows (status === 'running') show "Running" indicator instead of Start button
- Compact rows, simple layout

**Empty state:**
- When snapshot.workflows.length === 0 and no error: show instructional message
- Message: "Load a workflow to get started" with description "Use the button above or drag and drop a .WFmasterX or .json file"
- Keep the "+ Load Workflow" button visible in both empty and list states (at the top)

**Styling (HomeScreen.module.css):**
- Use CSS Modules (not plain CSS classes)
- Use @container device-frame queries (not media queries) for responsive adjustments
- Padding, spacing consistent with the app's existing style (see HeaderBar.module.css for reference: 12px-16px padding)
- Drop zone highlight: dashed border, light background color on drag over
- Error message: red text, small font
- Workflow list items: padding, border-bottom separator
- Start button: simple styled button
- Empty state: centered, muted text

**Imports needed:**
- processWorkflowFile, isProcessingError from '../../manager/fileProcessing'
- useWorkflowManager, useManagerSnapshot from '../../manager/useWorkflowManager'
- Plus, Upload icons from lucide-react
- useState, useRef, useCallback from react
  </action>
  <verify>
npx tsc --noEmit && npx vite build
  </verify>
  <done>
HomeScreen renders with load button, drag-drop zone, workflow list showing loaded workflows, empty state when no workflows loaded, and error display for invalid files. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vite build` succeeds
- HomeScreen.tsx imports and uses processWorkflowFile, useWorkflowManager, useManagerSnapshot
- File input accepts .WFmasterX, .json, .WFmaster
- Drag-and-drop events have preventDefault
- Error state displayed for invalid files and duplicates
</verification>

<success_criteria>
- Home screen displays a list of loaded master workflows
- User can load workflow files via button (file picker) and drag-and-drop
- Empty state shows instructional message
- Errors (invalid files, duplicates) are displayed to the user
- All styles use CSS Modules with container queries
</success_criteria>

<output>
After completion, create `.planning/phases/06-home-active-commands/06-01-SUMMARY.md`
</output>
