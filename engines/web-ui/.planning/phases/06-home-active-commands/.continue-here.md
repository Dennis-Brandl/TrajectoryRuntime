---
phase: 06-home-active-commands (bugfix)
task: bugfix
total_tasks: 3 bugs
status: fixes_applied_needs_testing
last_updated: 2026-03-13
---

<current_state>
Three bugs reported during user testing of Phase 6 (Active screen). All three fixes have been coded, TypeScript passes, build succeeds. Fixes are NOT committed — awaiting user verification.
</current_state>

<completed_work>

- Bug 1 (Images flash/disappear on Tablet/Desktop): Fixed in FormRenderer.tsx
- Bug 2 (Content too far down on Phone): Fixed in DeviceFrame.module.css + ActiveStepCard.module.css
- Bug 3 (Missing complete buttons): Fixed in App.css (added all missing form element styles)
- Phase 7 CONTEXT.md: Created and committed (separate from bugfixes)
</completed_work>

<remaining_work>

- User needs to visually verify all three fixes across Phone/Tablet/Desktop frames
- If fixes pass verification, commit the changes
- If any fix doesn't work, iterate
</remaining_work>

<decisions_made>

- FormRenderer canvas changed from position:relative + spacer div → position:absolute inside correctly-sized container. Root cause: double-height counting and overflow:hidden clipping on tablet/desktop where scale > 1.
- Phone padding-top reduced 50px → 44px (dynamic island bottom is at 42px from content top, 2px buffer).
- Card top padding reduced 16px → 8px to bring content closer to titles.
- Added comprehensive form element CSS in App.css (global styles, not CSS modules) because form elements use plain class names not CSS module references. Styles added for: .btn, .el-button, .btn-yes, .btn-no, .step-yesno, .step-yesno-buttons, .step-ui, .el-text-input, .el-checkbox, .el-radio, .el-header, .el-text, .el-image, .el-divider.
</decisions_made>

<blockers>
None — fixes compile and build clean.
</blockers>

<context>
The user was testing Phase 6 output (Active screen with form rendering) and found three visual bugs. All form element CSS was completely missing from the codebase — the element components had class names but no corresponding CSS anywhere. The FormRenderer had a layout bug where position:relative + spacer div double-counted height, causing the scaled canvas (scale > 1 on tablet/desktop) to overflow the overflow:hidden viewport chain.

Phase 7 CONTEXT.md was also gathered in this session (History + Overview screens discussion) and committed separately.
</context>

<next_action>
Start with: Ask user to test the three fixes by loading a workflow with images and buttons on all three device sizes (Phone, Tablet, Desktop). Run `npx vite` dev server if not already running. Verify:
1. Images render correctly on all device sizes (no flash/disappear)
2. Content starts just below headers on Phone (reduced gap)
3. Buttons appear and are styled on YES_NO and USER_INTERACTION steps
</next_action>

<files_modified>
- engines/web-ui/src/components/FormRenderer.tsx — canvas layout fix (position:absolute)
- engines/web-ui/src/components/shell/DeviceFrame.module.css — phone padding 50→44px
- engines/web-ui/src/components/ActiveStepCard.module.css — card top padding 16→8px
- engines/web-ui/src/App.css — added ~160 lines of form element CSS
</files_modified>
